module Game_graphics where

import Graphics.Gloss
import Config
import Types

start_life :: IO ()
start_life = play window backgroundcolor numSimStep 

window :: Display
window = InWindow
	"Conway life"   -- window text
	(800, 600)      -- window size
	(0, 0)          -- window offset

numSimStep :: Int
numSimStep = 5

initWorld :: 

